VL Credit Splitting / UPTO Conversion Feature Removal
======================================================
Date: 2026-02-23

GOAL: Remove the entire VL credit splitting/UPTO conversion feature.
- When VL credits are insufficient, block approval (should_deduct: false)
- Clean up existing linked companion UPTO records via migration
- Remove all related UI, backend logic, model fields, and tests

FILES TO MODIFY:
=================

1. app/Services/LeaveCreditService.php
   - Simplify checkVlCreditDeduction() to remove convert_to_upto/partial_credit branches
   - Return should_deduct: false when credits insufficient
   Status: [x] DONE

2. app/Http/Controllers/LeaveRequestController.php
   - Simplify handleVlApproval() (remove convert_to_upto/partial_credit handling)
   - DELETE handlePartialVlApproval() method entirely
   - DELETE createLinkedUptoRequest() method entirely
   - Clean show() - remove linkedRequest/companionRequests eager loading & props
   - Clean store() - remove whereNull('linked_request_id') from overlap check
   - Clean update() - remove whereNull('linked_request_id') from overlap check
   Status: [x] DONE

3. app/Models/LeaveRequest.php
   - Remove linked_request_id, vl_credits_applied, vl_no_credit_reason from $fillable
   - Remove from casts()
   - Remove linkedRequest() and companionRequests() relationships
   Status: [x] DONE

4. NEW MIGRATION: Drop columns + delete companion UPTO records
   - Delete companion UPTO records (where linked_request_id IS NOT NULL)
   - Drop columns: linked_request_id, vl_credits_applied, vl_no_credit_reason
   Status: [x] DONE (2026_02_23_222708_remove_vl_credit_split_fields_from_leave_requests_table.php)

5. resources/js/pages/FormRequest/Leave/Show.tsx
   - Remove VL Credits Not Deducted alert
   - Remove Partial VL Credits alert
   - Remove VL→UPTO conversion alert
   - Remove Linked Parent Request alert
   - Remove Companion UPTO Requests alert
   - Remove convert_to_upto/partial_credit alerts in approve dialog
   - Remove same in force approve dialog
   - Clean TypeScript types (LeaveRequest interface, Props)
   - Show "Insufficient VL credits" warning + disable approve when credits insufficient
   Status: [x] DONE

6. TESTS
   - Updated LeaveCreditServiceTest.php (6 VL tests renamed/updated)
   - Updated LeaveRequestDayStatusTest.php (removed companion UPTO assertion)
   - Updated LeaveRequestControllerTest.php:
     * Renamed Phase 5 header
     * Simplified VL full credits approval test
     * Replaced partial credits test → blocks approval test
     * Replaced zero credits test → blocks approval test
     * Deleted linked UPTO validation test
     * Updated credit preview show page test (removed partial_credit/upto_days/linkedRequest/companionRequests)
     * Deleted companion requests show page test
     * Replaced ineligible VL test → blocks approval test
     * Replaced fractional VL credits test → blocks approval test
     * Replaced fractional-only credits test → blocks VL test
     * Removed linked_request_id references from SL tests
     * Renamed SL partial credits test
   Status: [x] DONE

7. VERIFICATION
   - vendor/bin/pint --dirty            → PASS (13 files)
   - npm run types                      → PASS (2 pre-existing errors, none from our changes)
   - php artisan migrate                → PASS (migration applied successfully)
   - php artisan test (targeted):
     * LeaveRequestControllerTest.php   → 31 passed (202 assertions)
     * LeaveCreditServiceTest.php       → all passed
     * LeaveRequestDayStatusTest.php    → all passed
   Status: [x] DONE

PROGRESS LOG:
=============
- Steps 1-7 complete. All changes implemented and verified.
- Migration is production-safe: uses `php artisan migrate` (incremental), NOT `migrate:fresh`.
